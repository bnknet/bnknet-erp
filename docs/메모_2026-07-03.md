# 작업 메모 2026-07-03

> 가동 3일차. 근태·결재·카드·매출 운영 개선 + 카드 한도 처리 방식 정리.

## 1. 근태 기본 조회 오늘로 (PR #92)
- 출·퇴근 기록 목록 기본 조회를 이번달 전체 → 오늘~오늘. (기간 넓히려면 필터/이번달 버튼)

## 2. 결재 승인메모 배지 + 카드 상세 UI + 담당자·카드 표기 (PR #93)
- **승인 메모 배지**: 승인 지시·요청사항 메모 있는 결재 건에 목록에서 📝 표시(데스크탑·모바일)
- **결제 상세 모달 잘림 수정**: 화면 100%에서도 안 잘리게(상단 정렬 + max-h 스크롤, 긴 품목은 내부 스크롤)
- **담당자·카드 표기**: 결제카드를 '담당자 · 카드명'(예: 조현상 · 국민카드)으로
  — 결재 문서 상세, 결제 캘린더 상세/기간조회/결제상세 모달

## 3. 카드 한도 처리 방식 정리 (논의 — 개발 없음)
- 결론: 현재 로직 그대로가 정확. 이중복구 없음
  - **6/30 이전 구매** → 결제일에 그 카드로 **선결제(한도복구)** 올려 승인 → 한도 복구
  - **6/30 이후 구매** → **카드구매(매입)** → 차감, 결제예정일 지나면 자동 복구
  - 두 경로가 안 겹쳐서 이중복구 없음. 6/30 이전분 소진되면 이후 건만 남음
- 회사 프로세스: 카드매입=매입품의서(카드구매)로만, 선결제=선결제 탭, 카드값 나가는 날 지출결의서는 기록용(한도 무관)

## 4. 지출결의서 결제카드·구매처 란 제거 (PR #94)
- 지출결의서에 카드 연결로 한도가 잘못 차감되는 소지 원천 차단
- 결제카드/구매처 입력란은 카드구매(선결제 포함) 전용 → 지출결의서엔 미노출
- 저장 시 지출결의서는 card_id·purchase_vendor·payment_due_date 강제 null
- 역할 정리: 매입품의서(카드구매)=한도차감 / 선결제=한도복구 / 지출결의서=지출기록(한도 무관)

## 5. 매출현황 브랜드별 매출 사업자 필터 (PR #96)
- 사업자 버튼 선택 시 브랜드별 매출도 그 사업자 현재분으로 필터
- 과거 브랜드실적(brand_sales)은 사업자 구분 없어 전체일 때만 합산(특정 사업자 선택 시 제외)
- 7월부터 직접 변환·등록하는 매출은 사업자별로 정확히 분리됨(과거는 데이터 한계 — 인지됨)

## 6. 대화·업무 지침 저장 (PR #95, #97)
- docs/대화_업무_지침.md: 다정한 존댓말 말투 + 업무 응답 형식 + 워크플로우 + 개발 원칙
- 이모지는 아주 가끔만(톤다운)

## 7. 결재 상신자 수정/상신취소 + 대표·실장 전용 이력 (PR #99)
- 상신자 본인이 결재중·반려 문서를 수정(재상신, 결재라인 초기화) 또는 상신 취소(status=canceled) 가능
- 변경 이력(수정재상신·상신취소·승인철회)을 approval_logs에 기록, 상세에서 대표·실장만 조회(🔒)
- SQL: db/approval_logs.sql

## 8. 실장 승인 철회 (PR #100)
- 실장이 승인 후 대표 최종결재 전이면 '승인 철회'로 되돌려 재결재 대기 → 반려/수정 가능
- 대표 최종결재 후에는 불가(안전). approval_logs에 '승인철회' 기록

## 9. 인사관리 접근 제한 + 연봉/급여정보 (PR #101, #103)
- 인사 관리 접근을 대표·실장(ceo/admin)만 (페이지 가드 + 사이드바 roles)
- 직원 연봉(salary) + 급여일(pay_day) + 급여통장(salary_bank) 필드 추가, 폼 입력·상세 표시
- 6명 값 반영. SQL: db/employee_salary.sql, db/employee_pay_info.sql

## 10. 매입품의서 결재일자 승인 게이트 (PR #102)
- 카드구매 매입품의서(비선결제)는 settle_date(결재일자) 이후에만 승인 가능
- 발의(구매일)와 카드사 확정 매입금액(청구할인 등, 영업일 지연) 시점 차이 대응
  → 결재일 전까지 담당자가 금액 확인·수정. 승인/빠른승인 게이트, 반려는 상시 가능
- 선결제·지출결의서·휴가는 영향 없음

## 11. 결재 목록 상세 조회 필터 + 표시 건수 (PR #104)
- 사업자·문서종류·작성자·발의일(기간) 필터 (상태 필터와 함께 적용)
- 표시 건수 선택(기본 10 / 30 / 50 / 100 / 전체), 총 N건·상위 M건 안내, 초기화 버튼

## 실행 SQL (7/3)
- db/approval_logs.sql (approval_logs) — 실행 완료
- db/employee_salary.sql (employees.salary + 6명)
- db/employee_pay_info.sql (employees.pay_day, salary_bank + 6명)

## 12. 공지사항 MD 작성 권한 (PR #105)
- 공지 등록·수정·삭제 권한을 대표·실장 + MD(md 역할, 손사빈 과장)로 확대

## 13. 매출현황 상품별 매출 추가 (PR #106)
- 브랜드별 매출 하단에 상품별 매출 표(상품명·수량·매출·공헌이익·공헌이익률)
- 대표상품명(matchProduct) 기준, 사업자 필터·기간 반영, 수량 많은 순(상위 100)

## 참고: 카드 한도 운영 확정 (7/3 논의)
- 매입품의서(카드구매)=한도 차감(매입), 선결제=한도 복구, 지출결의서=지출 기록(한도 무관, 카드란 제거됨)
- 6/30 이전 구매분: 결제일에 그 카드로 '선결제(한도복구)' 올려 승인 → 한도 복구
- 매입품의서 승인은 결재일자(카드 확정매입일) 이후 가능 → 담당자가 확정금액 확인·수정

## 14. 직접주문 예상 공헌이익 표기 (PR #109)
- 도매 직접주문 등록 시 '예상 마진(확정 저장)' → '예상 공헌이익' + 예상 공헌이익률 표기

## 15. 미차감 배너 상세 + 재출고 매칭 반영 (PR #109)
- '재고 미차감 주문 N건' 배너 클릭 시 어떤 주문(상품·사업자·수량·매칭여부)인지 목록 표시
- 재출고 시 방금 추가한 매칭까지 반영(loadDbMatches(true)) + 대표명↔재고 정규화 폴백

## 16. 상품매칭 수량꼬리표 처리 (PR #108, #111)
- 수집상품명 끝의 수량표기 자동 제거: `(N개)`뿐 아니라 `, N개`·`, N개(N개)` 형태까지
- **핵심 버그 수정(#111)**: DB 매칭 collect_name에 수량꼬리표가 남아 있으면
  실제 주문명과 안 맞아 매칭이 아예 안 걸림 → 재고 차감 실패(덴프스 SJ글로벌 사례).
  matchProduct가 꼬리표 제거 정규화 키로도 색인하게 고쳐 흡수. **DB 수정 불필요.**
- 교훈: 재고 미차감 원인은 대표명↔재고 이름이 아니라 **수집상품명→대표명** 단계였음.
  (대표명·재고명은 처음부터 동일) 추측 말고 데이터(쿼리)로 먼저 확인할 것.

## 17. 공지사항 댓글 기능 (PR #111)
- 공지 상세에서 직원 누구나 댓글 등록(Ctrl/⌘+Enter), 본인·대표·실장만 삭제
- 공지 목록에서 각 글 옆 댓글 수 뱃지(💬 N) 표시
- 댓글 작성자 옆 직급 표시: employees.position 우선, 없으면 role 라벨(대표/실장/MD…)
- SQL: db/notice_comments.sql (**실행 필요**)

## 배포 원칙 (중요 — 이번에 놓쳐서 명시)
- 브랜치 푸시만으론 운영(bnknet-erp.vercel.app = main) 반영 안 됨
- **반드시 PR → main 병합**해야 Vercel 배포됨. "완료"는 병합·배포 확인 후에만.
- SQL 필요한 기능은 SQL 실행 안내까지 해야 진짜 완료.

## 18. 공지 목록 댓글 수 뱃지 + 작성자 직급 (PR #112)
- 공지 목록 각 글 옆 댓글 수(💬 N) 뱃지, 댓글 등록·삭제 시 갱신
- 댓글 작성자 옆 직급: employees.position 우선, 없으면 role 라벨

## 19. 슬랙 ERP 비서 봇 MVP (PR #113) — ★다음 이어서 진행★
- 대표·실장이 슬랙 DM으로 ERP를 자연어로 물으면 Claude가 읽기전용 조회·추론해 답하는 봇
- 코드: `src/app/api/assistant/route.ts` (Next.js API 라우트, runtime nodejs)
  - 흐름: 슬랙 이벤트 → 서명검증(HMAC) → 화이트리스트 → Claude(claude-opus-4-8, adaptive)
    가 `query_erp`(읽기전용 PostgREST) 도구로 Supabase 조회 → chat.postMessage로 DM 답변
  - 보안: 권한은 백엔드 강제(화이트리스트), GET 전용·rpc 차단, anon 키(앱이 읽는 것만 읽음)
  - 슬랙 3초 응답 제한 대응: 즉시 200 ack + `after()`로 비동기 처리
- 의존성: @anthropic-ai/sdk 추가(설치·lockfile 반영·빌드 통과 확인됨)
- 엔드포인트: https://bnknet-erp.vercel.app/api/assistant
- **가이드 문서: docs/슬랙봇_구축_가이드.md (A~E 단계)**

### ⏭️ 다음 재개 시 (봇 살리기 — 대표가 할 일, 나는 못 함)
1. Anthropic API 키 발급 (console.anthropic.com, 사용량 과금) → sk-ant-...
2. 슬랙 앱 생성(api.slack.com/apps): Signing Secret, Bot Token(xoxb-),
   스코프 chat:write/im:history/im:read/users:read, Event Subscriptions에 message.im 구독,
   Messages Tab 활성화
3. 대표·실장 슬랙 User ID(U...) 확보
4. Vercel 환경변수 4개 등록 후 Redeploy:
   ANTHROPIC_API_KEY / SLACK_SIGNING_SECRET / SLACK_BOT_TOKEN / SLACK_ALLOWED_USER_IDS
5. 슬랙 앱 Event Subscriptions Request URL = 위 엔드포인트 → 검증
6. **DM으로 함께 실제 테스트**하며 다듬기 (엔드투엔드 아직 미검증 — "된다"는 실제 답 확인 후)
- 이후 로드맵: 감사 로그 전체 기록 / 채널별 주제 격리(범위 등록) / 역할별 도구 제한(팀 확장)
  / 텔레그램 개인비서(같은 두뇌 재사용, 입출구만 텔레그램) / 아침 브리핑 푸시

### 슬랙 구조·설계 합의 (논의 결론)
- 봇 여러 개 X → **봇 하나 + 내부에서 주제·권한 분기**(역할별 도구 게이팅)
- 개인 질의=DM(완전 비공개), 공유=채널(@봇). 팀 배포해도 프롬프트 안 새어나감
- 채널은 주제 범위를 등록해두면, 권한 많은 사람이 물어도 그 채널 밖 데이터는 안 뜸(→ DM으로)
- 대표=전체권한: 슬랙 설치 한 번이면 DM 가능 + 백엔드 역할표에 ceo 1회 등록
- 봇은 API로 동작 → 대표 개인 claude.ai 대화와 완전 분리(직원이 못 봄)
- 감사 로그는 "누가 무엇을 조회했나"(운영 목적), 사생활 감시 아님. 이번 방침: 전체 기록

## 상태 / 팔로업
- PR #92~#113 (+메모 PR) 모두 main 머지·반영 완료
- SQL 확인: approval_logs·employee_salary·employee_pay_info 실행 완료 / product_matches 운영중
  / **notice_comments 실행 필요**
- ★진행중: 슬랙 ERP 비서 봇 — 대표가 슬랙앱·키·환경변수 설정 후 함께 테스트 (위 19번 참고)
- 장기 팔로업: 판관비 반영 진짜 영업이익, 보안 RLS 마이그레이션, 봇 팀확장·텔레그램
