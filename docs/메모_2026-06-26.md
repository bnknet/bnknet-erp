# 작업 메모 — 2026-06-26

> 이 날 나눈 질문/답변과 결정사항 정리. (대부분 운영·환경 관련 Q&A, 코드 변경은 없음)
> 컴퓨터 종료 전 기록용. GitHub에 저장됨.

---

## 1. 대화 기록 vs 작업물(코드) — 헷갈리지 말 것

- **작업물(코드)** = GitHub(git 커밋)에 저장 → **안전. 안 사라짐.**
  - 확인 결과: 로컬 = GitHub 완전 일치(커밋 `d388efb`), **총 50개 커밋 전부 보존**.
  - 만든 모듈 폴더 전부 그대로: inventory / sales / attendance / approval / partners / cards / products / hr 등.
- **대화 기록(채팅 로그)** = 세션·계정에 묶여 보관 → 환경 바뀌면 안 보일 수 있음(고장 아님).
- 맥락 유지 안전장치 = **PROJECT_PLAN.md "6. 진행 기록"**. 대화가 끊겨도 이 파일만 읽으면 작업 이어짐.

## 2. CLI(커맨더) vs 웹 Claude Code

- 교육은 **터미널(CLI)** 로 진행. 웹과는 다른 세션.
- **연습은 ERP 폴더 말고 별도 연습 폴더에서** 할 것 (예: `claude-practice`).
  - Claude Code는 "열린 폴더 안"에서만 동작 → 연습 폴더에서 하면 ERP 프로젝트 안 건드림.
  - 연습 폴더는 bnknet-erp 저장소와 연결하지 말고, **실수로라도 push 금지.**

## 3. 플러그인(plugin)이란?

- Claude Code에 기능 묶음(명령어·에이전트·자동화)을 추가 설치하는 것.
- `/plugin marketplace add <주소>` = 플러그인 "가게" 등록.
- 예시로 받은 `fivetaku/gptaku_plugins` = 한국 개발자가 만든 14개 플러그인 모음(연습용).
- ⚠️ **`/plugin`은 웹에선 안 됨. CLI(터미널)에서만 동작.**
- ⚠️ **제3자(외부인) 플러그인** → 그 사람 코드가 내 PC에서 돌 수 있음.
  **연습 폴더에서만 설치, 실제 ERP 작업 폴더엔 설치 금지.**

## 4. Slack + ERP AI 챗봇 구상 (향후)

- 구상: 슬랙에서 "5월 6월 매출·영업이익 비교해줘" → AI가 ERP 데이터로 정리/답변. 챗봇·에이전트.
- 구조: 슬랙 질문 → 백엔드가 **Supabase에서 실제 숫자 조회** → 계산은 코드/DB가 정확히 → **Claude API는 질문 해석+결과 설명만**(AI 암산 금지) → 슬랙 답변.
- **상태 = 보류(2026-06-24 결정과 동일).** 이유: AI는 질문 1건당 토큰 **과금**. 슬랙으로 하든 ERP 안에서 하든 비용은 동일.
- **순서: 7/1 매출 대시보드 먼저 가동 → 데이터 안정화 후 → 슬랙 챗봇 추가(이때 실제 단가·월 예상비용 보고).**
- 권장: Claude API 직접 연결(한국어·보안 통제 유리). 키는 `.env` 분리, 읽기 전용.

## 5. 구글 클라우드 이전 — 역할 분담

| 용도 | 저장 위치 | 예시 |
|------|----------|------|
| 숫자/표 데이터(계산·집계) | **Supabase(ERP DB)** | 매출, 재고, 영업이익 |
| 파일 보관(문서·이미지) | **구글 드라이브/클라우드** | 정산 엑셀, 영수증, 계약서 |

- 둘 다 필요. "엑셀 파일 자체"는 드라이브, "그 안의 숫자"는 ERP DB.
- 이 환경에 구글 드라이브 연동 이미 있음 → 향후 "드라이브 엑셀 → ERP 적재" 가능.

## 6. Supabase 데이터 안전성 + 백업 (중요)

### 안전성
- Supabase 데이터는 **클라우드(외부 서버)** 저장 → PC 끄기/폴더 변경/CLI·웹 전환과 **무관, 안 날아감.**
- **Supabase Pro(유료) 결제 중** → 매일 자동 백업 + 일시정지 위험 없음.
- 유일한 실질 위험 = **사람이 SQL로 실수로 삭제(DELETE/DROP)** 또는 RLS 잘못 풀기.

### 수동 백업 방법 (한 달 1회 규칙)
- **방법 A (쉬움, 정기용) ⭐**: 대시보드 → Table Editor → 테이블 선택 → Export → **Export as CSV**.
  주요 테이블 반복: orders / inventory / inventory_logs / employees / partners / cards / approvals.
  폴더명 `백업_YYYY-MM-DD`.
- **방법 B (전체 통백업, 완전 복구용)**: Project Settings → Database → Connection string 복사 →
  `pg_dump "연결문자열" -F c -f bnknet_erp_백업_YYYY-MM-DD.dump`
  (기술적 → 어려우면 7/1 직전에 함께 진행)

### 구글 드라이브에 백업 올릴 때 보안 (2겹)
- **1겹 — 파일 암호화(가장 강력)**: 백업 폴더를 **비번 압축(7-Zip, AES-256)** 후 그것만 업로드.
  비밀번호는 드라이브에 같이 올리지 말고 따로 보관(대표·실장만).
- **2겹 — 계정/폴더 권한**: 구글 계정 **2단계 인증(2FA)** 켜기 / 백업 폴더 공유 **"제한됨(대표·실장만)"** /
  **"링크 있는 사람 누구나" 절대 금지** / 가능하면 회사 Workspace 계정 사용.

### 백업 운영 규칙(요약)
- 매달 1회: CSV 백업 → 비번 압축 → 제한공유 드라이브 폴더 업로드.
- 7/1 직전 1회: 전체 .sql 백업으로 "정식 가동 기준점" 만들기.

---

## 7. 향후 Slack AI 봇 구상 (나중에 적용 — 요구사항 메모)

> 강의 강사가 법률검토 봇·비서 봇 등 여러 AI 봇을 만든 걸 보고 우리도 적용하고 싶음.
> 멀티 에이전트(다중 에이전트)로 시장조사·콘텐츠 자동화도 슬랙에 붙일 수 있음.

### 만들 만한 봇 (건기식·화장품 유통, 직원 7명 기준)
| 봇 | 하는 일 | 효용 |
|----|---------|------|
| 매출 질의 봇 | "5월 6월 매출·영업이익 비교" → ERP DB 조회·정리 | 대표·실장 이동 중 슬랙 확인 |
| 광고법규 검토 봇(=법률검토 봇 우리버전) | 상세페이지·마케팅 문구를 **식약처 표시광고 규정**으로 검토 | 건기식·화장품 과장광고 제재 예방 |
| MD 시장조사·콘텐츠 봇 | 경쟁사·트렌드 조사, 상세페이지 초안 생성(멀티 에이전트) | 상품기획 속도 ↑ |
| 비서 봇 | 일정·리마인더, 결재대기·재고부족 알림 | 챙길 거 자동 알림 |
| ERP 알림 봇 | 재고 자동저장 누락/결재 대기를 슬랙 푸시 | 기존 이메일 알림 → 슬랙 확장 |

### 멀티 에이전트(자동 팀) — 시장조사·콘텐츠 자동화
- 대장 에이전트가 전문 팀원 에이전트(시장조사/콘텐츠작성/검수)를 자동 생성 → 병렬 작업 → 결과 통합.
- 아까 본 플러그인에 이미 있음: `kkirikkiri`(에이전트 팀 구성), `pumasi`(병렬 PM), `insane-research`(멀티 에이전트 리서치).
- ⚠️ **토큰 가장 많이 먹는 방식** → 일상 X, 필요할 때만. ERP 핵심과 분리. 결과는 사람 검수(특히 광고 법규).

### 공통 원칙 (계획서와 일관)
- 계산은 코드/DB가 정확히, **AI는 질문 해석 + 결과 설명만**(AI 암산 금지).
- **질문당 과금** → 비용 발생. 키 `.env` 분리, 권한자만 읽기 전용.
- 권장: Claude API 직접 연결(한국어·보안 통제 유리).
- **도입 순서: 7/1 ERP 매출 대시보드 안정화 → 그 뒤 슬랙 봇 추가(실제 단가·월비용 보고 후).**

### 결정 필요 사항
- 현재 사내 소통 = **JANDI**(계획서 기록). 슬랙 새로 도입 여부는 별도 결정사항.

### ⭐ 슬랙 작업 착수 시 — 반드시 먼저 할 것 (사용자 지시, 2026-06-26)
> **이 메모를 읽는 Claude는, 사용자가 "슬랙 작업 시작하자"고 하면 코드부터 짜지 말고
> 아래를 먼저 사용자에게 꺼낼 것.**
1. **"에이전트를 어떤 식으로 만들지부터 같이 설계하자"** 고 먼저 제안 (봇/에이전트 구조 설계가 1순위).
   - 어떤 봇부터? (매출질의 / 광고법규검토 / 시장조사 / 비서 / 알림 중 우선순위)
   - 각 봇의 입력·처리·출력 흐름, ERP DB 연동 범위, 권한.
2. **토큰/범위 제한은 미리 두지 말 것.** 데이터가 얼마나 쌓일지 모르니, 착수 시점의 실제 데이터 양을 보고 그때 깊이·비용을 정한다. (오늘 데모는 일부러 작게 돌린 것일 뿐, 본 작업은 제한 없이 설계부터)

### ⭐ 스킬(skill) 추천 트리거 (사용자 지시, 2026-06-26)
> **이 메모를 읽는 Claude는, 작업 중 적합한 스킬/플러그인이 있으면 사용자가 묻지 않아도 먼저 추천할 것.**
> 배경: 강의에서 "시장조사 → HTML로 만들어줘"라니까 미리 등록된 디자인 스킬이 자동 적용되는 걸 봄.
> 사용자 요청: 필요할 만한 스킬을 내가 먼저 추천 + 우리 구조(건기식·화장품 유통 + ERP)에 어울리는 스킬도 추천.

- 스킬이란: 자주 쓰는 작업 방식을 미리 등록 → 매번 설명 없이 그 방식대로 자동 처리(예: 디자인 자동 적용).
- 우리 구조에 어울릴 후보(착수 시 재검토):
  - 디자인 스킬 — 시장조사·매출 리포트를 보기 좋은 HTML/대시보드로 자동 변환
  - 리서치 스킬(deep-research) — 멀티에이전트 시장조사 리포트(오늘 데모)
  - 엑셀/스프레드시트 스킬 — 정산·재고 엑셀 자동 가공(업무 핵심)
  - PRD/기획 스킬 — 새 모듈 기획서 자동 생성
  - 코드리뷰 스킬(code-review) — ERP 코드 품질·버그 점검(이미 사용 가능)
- ⚠️ 제3자 플러그인/스킬은 출처·보안 확인 후, **실제 ERP 폴더엔 신중히**(연습은 별도 폴더).

### 멀티 에이전트 데모 결과 (2026-06-26, 참고용 비용 감각)
- 건기식 3사(위바이옴/종근당건강/여에스더) 네이버 시장조사를 에이전트 3개 병렬 실행 → 비교표 생성 성공.
- 비용: 검색 4회 제한·effort 보통으로 **작게** 돌려 합계 약 9.2만 토큰(에이전트 3개), 전체 ~10만 토큰, 약 80초. 대략 5천~1만원 수준(Opus 기준, 변동).
- xhigh + 에이전트당 80k 토큰으로 깊게 돌리면 이 데모의 3~5배 이상. → **깊이로 비용이 결정됨.**
- 발견: 여에스더는 광고심의 '권고'·의사마케팅 윤리논란 있음 → 유통 시 광고문구 리스크 체크 필요(광고법규 검토 봇 활용 지점).

---

## 8. 노트북 교체 시 이전 방법 (PC 바꿔도 그대로 작업 이어가기)

> 핵심: 웹 Claude Code는 전부 **클라우드**에 있어서 옮길 "물건"이 거의 없음. 새 PC에 **로그인만** 하면 이어짐.

### 다 클라우드에 있음 (PC 바꿔도 안 사라짐)
- 코드 → GitHub / 데이터(매출·재고) → Supabase / 대화 → Claude 계정 / 작업맥락 → PROJECT_PLAN.md+메모(GitHub 안).
- 옛 노트북 고장·폐기해도 하나도 안 잃음.

### 새 노트북에서 할 일 (웹 작업)
1. 브라우저(크롬/엣지) 설치
2. claude.ai 로그인(기존 계정) → 대화·작업 바로 이어짐. 끝.

### 챙길 것 = 파일이 아니라 "계정 정보"
- Claude 계정 / GitHub 계정(**bnknet** — 개인계정 섞이면 403) / Supabase / 구글 계정.
- 각 계정 **2단계 인증(2FA) 수단**(인증앱 등) 미리 확인 — 기기 바뀌면 여기서 제일 막힘.
- API 키 등 비밀값은 클라우드 환경에 있어 PC엔 없음 → 웹 작업이면 키 옮길 것 없음.

### (참고) CLI도 새 PC에서 쓸 거면
- Node.js + Claude Code CLI 설치, Git 설치+GitHub(bnknet) 인증, `.env` 키 파일만 안전하게(암호화) 이전.

### ⚠️ 옛 노트북 처분 전
- 모든 계정 로그아웃 + 디스크 초기화(포맷). (매출·직원정보 다루던 기기 → 세션 남으면 위험)

---

## 다음에 이어서 할 일 (7/1 가동 전 남은 2차 작업)

- [ ] **3중 정합성**: 주문변환 시 재고 자동차감 + 취소/반품 RPC(주문상태+재고복구+로그 한 트랜잭션)
- [ ] **과거 매출 엑셀 적재**: 6월까지 실적 DB 적재(7/1에 전월 대비·추이 보이게) — 운영자 엑셀 필요
- [ ] **최종 재고 리스트 적재(6/30 예정)**: 부진재고 브랜드 `기타1` 표기 확인 후 반영
- [ ] 대시보드에 원가미입력/매출 경고 배너 연동
- [ ] (2차) 수수료·배송비·부가세 차감
- [ ] 보안 마무리: 하드코딩 키 제거, temp_anon_access 정책 삭제 → 로그인 권한 전환
