# 작업 메모 2026-07-06

> 가동 후 운영 개선 세션(7/4~7/6). 몰수수료 관리, 상품매칭 보강, 카드결제 폰에서 보기,
> PWA 업데이트 배너, 결재 선결제/매입 구분·워크플로우, **카드 매입 구매상품 건별 부분 선결제**.
> (직전 메모: 메모_2026-07-03.md — PR #113까지)

---

## A. 몰별 수수료 관리 (PR #115, #116)
- **#115 몰별 수수료 관리 메뉴**: `mall-fees` 페이지에서 몰별 수수료율 추가·수정·삭제. 변경 이력 `mall_fee_logs`.
- **#116 유튜브쇼핑 몰명 정규화**: 표기 흔들림(유투브/유튜브 등) → `normalizeMall`이 `Cafe24(신) 유튜브쇼핑` 하나로 통일.
- 파일: `src/lib/mallFees.ts`(normalizeMall, CANONICAL_MALLS, KNOWN_COMPANIES), `db/mall_fee_logs.sql`

## B. 상품매칭 보강 (PR #117, #118) — 7/3 #108·#111 수량꼬리표에 이어서
- **#117 자기 자신 매칭**: 입력이 이미 대표상품명이면 그 자신으로 매칭(정규화 키 색인).
- **#118 세트(BOM) 세트명 매칭**: `product_bom`의 set_name도 매칭으로 인정.
- 파일: `src/lib/orderConvert.ts`(matchProduct — PRODUCT_MAP_VAL_NORM, DB_MATCH_VAL_NORM, BOM_SET_NORM, setBomSets; loadDbMatches)
- 교훈(재확인): 재고 미차감 원인은 대체로 **수집상품명→대표명** 단계. 추측 말고 쿼리로 먼저 확인.

## C. 카드 결제내역 폰에서 바로 보기 (PR #120, #121, #122)
- 엑셀 앱 호환 문제 회피 — 화면 표 뷰 + 인쇄/PDF. (Z폴드 등에서 xlsx 안 열리던 문제)
- **#120** 결제예정/구매내역을 화면 표로 바로 보기(`tableViewModal`, viewSchedule/viewPurchase)
- **#121** 버튼 크고 문구 명확하게, **#122** 화면보기를 기본 액션·엑셀 다운로드는 보조 링크로.

## D. 첨부 엑셀/워드 폰에서 앱 없이 열기 (PR #124)
- 결재 첨부 xlsx/docx/pptx → Office Online 뷰어 URL(`view.officeapps.live.com/op/view.aspx?src=...`)로 열기.
- 파일명 누르면 화면에서 바로 보기(모바일은 대부분 확인용). 파일: `attachmentHref(url)` (ApprovalContent.tsx)

## E. PWA 새 버전 업데이트 배너 (PR #123, #126)
- **#123** 새 배포 감지 시 상단 파란 '새로고침' 배너. 원리: 빌드에 심긴 `NEXT_PUBLIC_BUILD_ID`
  (=VERCEL_GIT_COMMIT_SHA) vs 서버 `/api/version` 비교(5분 주기 + 탭 포커스 시).
- **#126** 배너 고정(× 제거 — 실수로 닫는 것 방지).
- 파일: `src/components/UpdateBanner.tsx`, `src/app/api/version/route.ts`, `next.config.ts`(env), layout.tsx

## F. 결재 카드 선결제/매입 구분 · 워크플로우 (PR #119, #125, #127, #128, #130, #133)
- **#119** 카드구매 승인 시 **선결제/일반 매입 구분 배지**: `cardKind(a)` — 선결제(파랑·한도복구) vs 카드구매매입(주황·한도차감).
- **#125** 선결제(한도복구) 결제일을 **실제 결제일(spend_date)** 로 지정 가능. 카드구매 form에서 선결제일 자동잠금 버그 수정(`!isPrepay` 게이팅).
- **#127** 대표 최종승인 건 **반려(결재 취소)** 기능 — `rejectFinalApproved()`.
- **#128** 문서종류 필터에 선결제/카드매입 세분(전체·지출결의서·카드매입·선결제·휴가).
- **#130** 카드매입 **결제예정일 수정(앞당겨 결제)** — 선결제 대체 수단. `computePaymentDate`로 원래 청구일 자동계산 버튼.
- **#133** 결제예정일 '원래 청구일 자동계산' 복원 도구 — 잘못 바꾼 결제예정일을 되돌릴 때 사용.
- 파일: ApprovalContent.tsx(cardKind, saveSpendEdit, saveDueEdit, rejectFinalApproved, recompute)

## G. 품목 엑셀 날짜 일련번호 버그 (PR #129) ★중요
- 증상: 품목 엑셀 업로드 시 날짜가 `46183`/`46205` 같은 **엑셀 일련번호**로 저장됨(월/일 안 나옴).
- 원인: 셀을 숫자로 읽음. 수정: `XLSX.read(buf,{type:'array',cellDates:true})` + `fmtItemDate(v)`가 Date/serial/텍스트 모두 MM/DD로.
- **기존 잘못 저장분 정정 SQL(실행 완료 7/6)**:
```
update public.approval_items set item_date = to_char(date '1899-12-30' + item_date::int, 'MM/DD') where item_date ~ '^[0-9]{4,6}$' and item_date::int between 40000 and 60000;
```
- 참고: 엑셀 일련번호 46183 = 2026-07-06 (기준 `date '1899-12-30' + serial`).

## H. ★카드 매입 구매상품 건별 부분 선결제 (PR #131, #132, #134, #135, #136) — 오늘의 핵심
### 배경/요구
- 07/06 구매는 **상품 건별 카드구매(한도차감)** 로 매입 결재. 나중에 한도가 필요하면
  그 매입의 **구매상품을 건별로 골라 선결제**(앞당겨 결제)하여 **그 항목 금액만 한도 복구**.
- 캘린더 목적 = 자금계획(몇월 며칠에 얼마 빠지나). 선결제분은 선결제한 날 출금, 결제예정일엔 줄어든 나머지 청구.

### 최종 동작(합의)
- **선결제 처리 모달**(한도 현황 탭 → 💚 선결제 처리): 미결제 매입 목록 → 매입 **금액 클릭 시 구매상품 상세 펼침**
  → 앞당겨 결제할 상품만 체크 → 처리 시 확인창 → 그 항목에 `prepaid_date/prepaid_at` 기록.
  - 이미 선결제/취소된 항목은 회색으로 잠금(이중처리 방지). 상세 품목 없는 매입은 전체를 결제예정일 앞당김(`appr:` 폴백).
- **한도 현황**: `미결제 매입 = 총액 − 취소분 − 선결제분(선결제일이 오늘까지 지난 것만 복구)`. 자동 연동.
- **캘린더(자금계획)**:
  - 결제예정일 청구액 = `총액 − 선결제총액`(선결제분 이미 빠진 실제 청구액; 0이면 미표시)
  - 선결제일 = `+선결제금액`(그날 실제 통장서 나간 출금)
  - → 두 날 합치면 전체금액, 이중반영 없음.
  - 취소·환불은 기존대로 환불일에 `−`(실제 카드는 청구 후 환불 흐름).
- **선결제 이력**: `선결제처리` 로그(누가/언제/무엇/얼마/선결제일) — 모달 내 이력 + 변경 로그 탭.
- **주의(설계)**: 선결제일 **기준**. 오늘=즉시 복구, 미래날짜=그날 복구.

### 커밋 흐름
- #131 선결제 처리(매입 단위, 한도 복구) → #132 확인창+건별 정보+이력
- #134 **구매상품 건별 부분 선결제**(prepaid_date/prepaid_at) → #135 예정일 청구액에서 차감 → #136 선결제일 출금(+) 표기
- 오늘 품질점검에서 추가: **선결제된 항목은 취소 모달에서도 잠금**(총액−취소−선결제 이중차감 방지)
- 파일: `src/app/cards/CardsContent.tsx`, `db/approval_item_prepaid.sql`

### 실행 SQL (7/6 — 실행 완료)
```
alter table public.approval_items add column if not exists prepaid_date date, add column if not exists prepaid_at timestamptz; create index if not exists idx_approval_items_prepaid on public.approval_items (approval_id) where prepaid_date is not null;
```

---

## I. 품질 점검(선결제 기능 독립 리뷰 + 수정) — 7/6
- 독립 리뷰어(에이전트)로 선결제 로직을 적대적 검증 → 실제 버그 수정 완료:
  - **[HIGH] 선결제+취소 동시 항목 한도 이중차감**: `취소분 + 선결제분`을 둘 다 빼면 한도가 과다표시(실제보다 커 보임 → 한도 초과 위험).
    → ① 취소 모달에서 **선결제된 항목은 취소 잠금**(체크 불가, '선결제' 표시). ② 계산에서도 취소된 항목 id는 선결제 합산에서 제외(방어).
  - **[MED] 선결제일 > 결제예정일 방지**: 앞당겨 결제인데 선결제일을 결제예정일보다 뒤로 잡으면 자금흐름 왜곡 → 처리 시 경고·차단.
  - **[MED] total_amount NaN 방어**: 한도 계산에서 `(p.total_amount || 0)` 처리(누락 시 대시보드 전체 NaN 되던 것).
- 남긴 소소한 한계(인지·문서화, 지금은 미수정):
  - 상세품목 없는 매입의 `appr:` 폴백은 결제예정일만 앞당김(선결제일 즉시복구 아님, 새 예정일 지나면 복구) + 완료표시 없음.
  - 선결제 이벤트 캘린더 상세 모달 하단 합계는 매입 전체 총액 표시(클릭한 항목 금액과 다를 수 있음 — 표시상).

## J. 모바일 UI 다듬기 (오와열·가독성) — 7/6 마무리
- **업무일지 사업자 필터**: 순서 `전체 / BNKNET / 더블아이 / SJ글로벌 / IX글로벌`로 변경.
  모바일 2열 그리드(전체는 상단 전체폭), 데스크탑은 인라인 → 칸 정렬 정돈. (WorklogContent.tsx)
- **행사·일정 캘린더 막대**: 좁은 모바일 칸에서 `16:00 토스미팅`이 시간에 밀려 제목이 잘리던 것 →
  막대엔 **일정명 우선 표시**(시간은 날짜 상세/툴팁에). (CalendarContent.tsx)
- **카드·매입 탭**: `변경 로그`가 세로로 깨지던 것(그 버튼만 whitespace-nowrap 누락) →
  4개 탭을 `flex-1` 균등폭·중앙정렬·한 줄로 깔끔하게. (CardsContent.tsx)
- **계정 관리 필터**: 사업자/카테고리 라벨을 위로 빼고 각각 **3열 그리드**(6개→2줄 균등)로 정렬,
  `+ 계정 추가`는 상단 우측으로. (AccountsContent.tsx)
- **카드 한도 현황 요약**(추가 다듬기): `잔여 N원`(크게) / `사용 N원` / `한도 N원` **3줄 분리** —
  큰 금액 뒤에 사용·한도가 붙어 어정쩡하던 것 정리. `+ 카드 등록` 버튼 줄바꿈 방지·정렬. (PR #139, #140)

## K. ★슬랙 ERP 비서 봇 — 실가동 성공 (PR #142)
- 대표 계정 세팅 완료 후 **엔드투엔드 첫 성공**(슬랙 DM → Vercel → Claude → Supabase 조회 → DM 답변).
- 대표가 직접 한 세팅: Anthropic 키 발급·$20 충전 / 슬랙앱 `ERP비서` 생성(스코프 chat:write·im:history·im:read·users:read, App Home Messages Tab) / Vercel 환경변수 4개(`ANTHROPIC_API_KEY`,`SLACK_SIGNING_SECRET`,`SLACK_BOT_TOKEN`,`SLACK_ALLOWED_USER_IDS=U0BD4E5UBUJ`) → Redeploy / Event Subscriptions URL 검증(Verified) + `message.im` 구독.
- 첫 실패("이번달 매출" → "(답변을 생성하지 못했어요)") 원인·수정(PR #142):
  1. **DB 스키마 미인지** → 시스템 프롬프트에 실제 테이블·컬럼·집계법 지도 주입(매출=orders.amount·canceled=is.false·기간 upload_date / 재고 inventory / 결재 approvals 등)
  2. **max_tokens 부족(4096)** → 8192 + effort medium, max_tokens로 답 못 낸 경우 한 번 더 이어서 정리
  3. **보안**: `accounts`(서비스 비밀번호) 테이블 봇 조회 차단(BLOCKED_TABLES)
- 파일: `src/app/api/assistant/route.ts` (todayKST 주입, SCHEMA_GUIDE, buildSystem)
- 비용 감(참고): 간단질문 ~25~40원, 데이터질문 ~200~400원, 무거운 것 ~500~700원. $20≈데이터질문 100~700회.
- ★남은 것: 실사용하며 자주 틀리는 패턴을 SCHEMA_GUIDE/규칙에 반영해 다듬기(사람이 개선). 다음 후보: 6월대비 7월 추론·목표달성 전망(sales_targets 활용), 아침 브리핑 푸시.

## L. 주문 변경 이력 접근 제한 + 변경 로그 전용 탭 (PR #144, #145)
- **#144 이력 대표·실장 전용**: 도매 주문 수정 모달·업로드 이력 상세 모달의 "변경 이력"을
  ceo/admin만 노출(🔒). 재고담당 등은 주문 수정은 되어도 감사 이력은 못 봄. (canViewHistory 게이트)
- **#145 변경 로그 탭**: 주문 페이지에 `🔒 변경 로그` 탭(대표·실장 전용) 추가 —
  등록·수정·취소·취소해제·부분취소/반품 전체를 표로(최근 300건: 일시/구분/주문 상품·몰·주문번호/내용/처리자).
  - ★보강: 취소류가 그동안 inventory_logs에만 남고 order_change_logs엔 없던 것 →
    취소/취소해제/부분취소·반품도 order_change_logs에 건별 기록(사유 포함). **지금부터 쌓임(과거 소급 X).**
  - 별도 SQL 불필요(order_change_logs 재사용). 파일: src/app/orders/OrdersContent.tsx
- 후속 후보(요청 시): 변경 로그 표에 수량·금액 컬럼, 기간/구분 필터.

## 활용 방향 논의 (개발 없음 — 다음에 이어서)
- 비서봇 활용 관심사: **한 달 지출을 카테고리별로 묶어 영업이익 개선 포인트** 찾기.
  · 지금도 approvals/approval_items의 금액·구매처·품목을 봇이 읽어 **추론으로 대략 분류** 가능(매입/광고/물류/수수료/기타).
  · 정밀하게 하려면 = '판관비' 숙제: 구매처→카테고리 매핑 or 결재에 카테고리칸 추가 → 그때부터 믿을 수 있는 영업이익 자료.
  · 한계(대표께 안내함): 봇은 **입력된 데이터만** 봄. 결재 첨부파일(카드내역 PDF/이미지) 자체는 안 읽음 →
    분석하려면 품목으로 입력돼 있어야 함. 결재 안 거친 지출은 안 보임.
- "쓸수록 똑똑" = AI 자가학습 아님. **사람이 SCHEMA_GUIDE/규칙을 개선**하는 것(+선택: 메모리 저장소 추가 가능).
- "자면서 학습" = 대부분 과장. 실체 있는 건 ① 예약실행(cron, 아침 브리핑) ② 메모리 축적 두 가지 — 원하면 추가.
- 아침 브리핑은 프롬프트만으론 안 됨(봇은 반응형·심장 없음) → **Vercel Cron(알람) + 프롬프트(내용)** 조합 필요.

## 배포 원칙 (재확인 — 이번에도 철저히)
- 브랜치 푸시만으론 운영 반영 안 됨. **반드시 PR → main 병합** → Vercel 배포. 병합·검증 후에만 "완료".
- 매 병합 후 브랜치 재동기화: `git fetch origin main && git checkout -B <branch> origin/main` → force-with-lease push.
- SQL 필요한 기능은 실행 안내까지 해야 진짜 완료. 데이터로 먼저 확인, 추측 금지.

## ⏭️ 내일 이어서 할 것 (2026-07-07 예정)
1. **비서봇 실사용 정확도 다듬기** — 자주 틀리는 질문 나오면 SCHEMA_GUIDE/규칙에 반영(사람 개선).
   6월 대비 7월 매출·목표달성 전망(sales_targets) 답 품질 확인.
2. **판관비/영업이익** — 지출 카테고리 태깅(구매처→카테고리 매핑 or 결재 카테고리칸) → 봇 영업이익 분석 정밀화.
3. (선택) 아침 브리핑 cron, 변경로그 표 컬럼 추가, 봇 메모리.
4. 보안 RLS 마이그레이션(장기).

## 상태 / 팔로업
- PR #115~#145 모두 main 머지·반영 완료. 실행 SQL(엑셀날짜 정정, approval_items prepaid) 완료.
- 슬랙 비서봇 실가동 중(대표 U0BD4E5UBUJ 화이트리스트). Anthropic $20 충전됨.
- **이어서 할 계획 작업(직전 메모에서 계속)**:
  1. ✅ 슬랙 ERP 비서 봇 — **실가동 성공(PR #142)**. 이후 실사용하며 SCHEMA_GUIDE 다듬기(K 참고)
  2. 판관비 반영 진짜 영업이익 (영업이익_판관비_기획.md)
  3. 보안 RLS 마이그레이션 (보안_현황및계획.md)
  4. 봇 고도화: 자주 틀리는 계산 규칙 추가, 아침 브리핑 푸시(cron), 팀확장·주제격리·텔레그램
- 알려진 소소한 한계(인지됨):
  - 선결제된 항목의 환불(선결제 후 반품)은 UI에서 잠가둠 → 필요 시 수동 처리.
  - 선결제일을 결제예정일보다 뒤로 잡는 건 비정상 입력(정상 흐름은 선결제일 ≤ 결제예정일).
