# 작업 메모 2026-08-10

> 주문변환 수량 인식 버그(수량 뒤 영문 꼬리표) 수정 + 이미 등록된 아세로라 8건 수량·재고 보정.

---

## A. 주문변환 수량 인식: 수량 뒤 영문 꼬리표(BN 등) 대응 (PR #293)
- **증상**: 더블아이(ESM지마켓/옥션) 데이즈온 그린 아세로라C 주문에서 옵션 2/3/4/6개가 전부 수량 1로 잡힘.
- **원인**: 수량은 상품명 끝의 "N개/박스/포/세트"를 끝-앵커(`$`) 정규식으로 추출. 이 상품은 `"…분말 2개 BN"`처럼 "N개" 뒤에 `BN` 꼬리표가 붙어 끝이 아니게 되어 인식 실패 → 수량 1.
- **수정**: `orderConvert.ts`의 수량 정규식(`extractQtyAndName`, `TRAIL_QTY_RE`)에 끝의 짧은 영문 꼬리표(`[A-Za-z]{1,4}`)를 허용.
  - `"…N개 BN"` → 수량 N 인식 ✅
  - `"…60캡슐 12개월분"`(개월분·뒤가 한글)·중간 "N개"·"…, 100ml" 등은 계속 미인식 → **회귀 없음**(node 테스트로 확인).
- 교훈: 끝-앵커를 무작정 풀면 "12개월분"의 "12개"를 오인식함 → 영문 꼬리표만 좁게 허용.

## B. 이미 등록된 아세로라 8건 수량 보정 (SQL)
- 코드 수정 전 등록된 8건은 quantity=1로 저장돼 있어 별도 보정.
- `db/orders_qty_fix_daysone_acerola_20260807.sql`: order_number로 quantity를 2/3/4/6으로 UPDATE(멱등).
- 주문번호/수량: 2154974008=2, 2154973989=3, 2154973993=3, 2154974016=3, 2154974014=4, 2154974007=4, 2154974015=6, 2154974013=6.

## C. 재고 정합성 보정 (핵심 — 재고는 저장식 차감)
- **중요 개념**: 재고 차감은 실시간 집계가 아니라 등록 시점 "자동출고(`/rpc/ship_orders`)"로 재고수량을 깎아 저장. `orders.stock_deducted` 플래그로 관리. → orders.quantity를 나중에 SQL로 고쳐도 재고는 자동 재차감 안 됨.
- **진단(데이터)**: 8건 모두 stock_deducted=true지만 등록 당시 quantity=1이라 각 1개씩만 차감(inventory_logs 합=1/건). 부족분 합계 = 23개.
  - 부족분: 2개→1, 3개(3건)→6, 4개(2건)→6, 6개(2건)→10 = 23.
- **보정 완료(사용자)**: 더블아이 아세로라 재고(id d4339ca2-9b39-4689-9c6f-e701381bed99) 850 → 827 (−23).
  - `update inventory set quantity = quantity - 23 where id = 'd4339ca2-...';`
- 결과: 기존 차감 8 + 추가 23 = 31 = 실제 주문수량 합과 일치. 수량·재고·매출/출고 집계 모두 정합.

## 실행 필요 SQL 현황
- [x] **완료(사용자)**: 아세로라 8건 quantity 보정 (`db/orders_qty_fix_daysone_acerola_20260807.sql`).
- [x] **완료(사용자)**: 아세로라 재고 부족분 23개 추가 차감 (inventory 850→827).

## 참고 / 다음
- 오늘 8건 외 다른 날짜 아세로라 주문은 문제 없다고 사용자 확인. (일회성 보정으로 종료)
- 앞으로 "N개 BN" 형태 주문은 코드 수정으로 변환 시 자동 정상 차감.

---

# 슬랙 ERP 비서 고도화 (오후 작업, PR #295~301)

배경: 대표님이 슬랙 Pro 도입 + 기존 커스텀 봇(`src/app/api/assistant/route.ts`, ERP 실시간 연동)을 직원 전체로 확대. 잔디 대체(대화) + 부서별 권한 AI 조회가 목표.

## D. 권한 게이팅 (PR #295, #296)
- 슬랙 계정 이메일 → `employees`(활성) 역할 → 조회 권한 판별. 매칭 실패 시 `SLACK_ALLOWED_USER_IDS` 화이트리스트는 전체로 폴백.
- 보안: `accounts` + `employees.password_hash`(비밀번호) 봇 조회 차단.
- 권한 거부 원인별 진단 메시지(①이메일 미확인 ②직원 미매칭 ③역할 미매핑).
- **전제조건(완료)**: 슬랙 앱에 `users:read.email` 스코프 추가 + 재설치. (이거 없으면 직원 이메일을 못 읽어 전부 거부됨) → 대표님 완료 확인.

## E. 집계 정확도 — "화면과 100% 일치" (PR #298, #299)
- **문제**: 봇이 orders를 200건까지만 받아 LLM이 직접 합산 → 이번달 매출 17,902,058원으로 답(실제 326,922,344원, 약 5%). 오보고 위험.
- **해결**: 전용 집계 도구를 서버에서 전량 페이징 + 화면과 동일 계산(`computeOrderLines` 등) 공유.
  - `sales_summary`: 매출(공급가액 ÷1.1)·공헌이익, 사업자별. 취소·실정산·몰수수료·원가·세트 반영. `loadDbMatches` 반영(공헌이익 정확도).
  - `stock_summary`: 재고 총수량·평가액(수량×원가)·사업자별·마이너스재고.
  - `outbound_summary`: 기간 출고수량(대표상품명+옵션 매칭, 취소/도매 제외 옵션)·미매칭.
  - `query_erp`는 200건 초과 시 "⚠️ 잘림" 경고 표시(조용한 오답 방지).
- **검증**: 매출·재고 모두 화면과 일치 확인(재고 IX 9개 차이는 실시간 출고 타이밍, 정상).

## F. 권한 체계 = ERP 메뉴 기준으로 통일 (PR #300, #301) ★현재 구조
- 채널봇 추가: 채널에서 `@ERP비서` 멘션 → **채널 기준 권한**(사람 무시, 멤버 전원이 답을 보므로). 미매핑 채널 기본 차단.
- 권한을 **메뉴 묶음(프로필)**으로 정의(`MENU_DEF`, `PROFILE_MENUS`). 채널·개인 공통.
  - **물류**: 매출현황·매출목표·주문변환·재고관리·상품마스터·거래처관리
  - **영업**: 물류 + 공지사항·업무일지 (재고관리 포함 → 강웅구 겸직 별도설정 불필요)
  - **경영지원**: 영업 + 행사일정·출퇴근·카드매입·결재 (인사관리=급여는 **제외**)
  - **전체**: accounts·rpc만 빼고 전부 (ceo/admin)
- 개인(DM): `EMAIL_PROFILE`(직원별) > `ROLE_PROFILE`(역할 기본: ceo/admin=전체, sales/md=영업, inventory=물류).
- 채널(@멘션): `CHANNEL_PROFILE`(채널ID→프로필. 코드 직접 + `SLACK_CHANNEL_SCOPES` 환경변수 병행).

## 슬랙봇 — 다음 할 일 / 대표님 TODO
- [ ] **팀봇 활성화(대표님)**: ① 앱에 `app_mention` 이벤트 + `app_mentions:read` 스코프 추가 후 재설치 ② 각 채널(#물류/#영업/#경영지원)에 `/invite @ERP 비서` ③ **채널 ID 3개 전달** → 코드 `CHANNEL_PROFILE`에 매핑 후 재배포.
- [ ] **직원별 개인봇 권한(대표님 전달 예정)**: "이름(이메일)→프로필" 형태로 주면 `EMAIL_PROFILE`에 반영. (기본은 역할값 사용 중)
- [ ] 박정진 메일 mqw10→**mqw100**로 재초대 완료. ERP employees 이메일도 mqw100 일치 필요(미확인 시 SELECT/UPDATE).
- [ ] (나중) 사용 가이드 문서 제작.
- 채널이 특이 조합 필요하면 새 프로필 추가 가능(메뉴 조합).
