# 작업 메모 2026-07-02 (종합)

> 가동 2일차 하루치 작업을 한 곳에 정리. 상세는 `메모_2026-07-02.md`(오전) / `메모_2026-07-02_오후.md`(오후) 참조.

## 오전
1. **수량표기 확인 안전망 A+B** (PR #76)
   - 송장 수량 치환 로직은 미변경(검증 완료). 감지·검수 계층만 추가
   - A: 변환 미리보기에서 개입/매/병/팩/1+1 등 미인식 단위 행 노란 하이라이트 + ⚠️
   - B: 변환 완료/저장 시 '수량 확인 필요' 경고 + `ship_alerts(kind=qty_check)` → 대시보드 배너
2. **재고 카테고리·브랜드 상품마스터 동기화 + 입출고내역 수기만 토글** (PR #77)
   - 재고 편집/일괄변경 시 카테고리·브랜드도 products 마스터에 동기화(기존엔 판매상태만)
   - 입출고 내역 기본 '수기만'(order_id null) 표시, 📝수기만/📋전체 토글
3. **상품 매칭 셀프 관리** (PR #78) ⭐
   - `product_matches` 테이블 + 변경 로그. 하드코딩 820 base + DB 매칭 병합(DB 우선)
   - `/product-matches` 화면(추가/수정/삭제·미리보기·이력), 미매칭 알림에 🔗매칭추가 바로가기
   - 재고·매출이 동일 matchProduct() 기준으로 동시 연동. SQL: db/product_matches.sql(실행 완료)
4. **7/2 오전 알림 대응** — 마이메이트/더마클라 재고 매칭 확인, 더블아이 마이메이트 −5 실사(완료)

## 오후
5. **도매 예상 마진 빨간색 강조** (PR #80)
6. **주문 삭제 시 재고 복구 + 매출 제외** (PR #81)
   - deleteOrdersWithRestore: 삭제 전 cancel_orders로 재고 원자적 복구 후 행 삭제(복구 실패 시 중단)
   - 조회·취소 '완전 삭제' + 도매 수정 모달 '🗑️ 이 주문 삭제' 버튼(대표·실장)
   - 기존 삭제 시 재고 미복구 버그 동시 수정
7. **카드 결제 캘린더 선결제 카드 오류 교정(데이터)**
   - 6/30 조현상 국민카드 선결제가 현대카드로 잘못 선택돼 저장됨 → card_id UPDATE로 국민카드 교정(검증 완료)
   - 조현상 card_type='조현상카드'는 의도된 홀더 그룹 — 손대지 않음
8. **결재 결제카드 교정 기능** (PR #82)
   - 결재 상세 → 💳 결제카드 옆 '카드 수정'(대표·실장)로 카드 잘못 선택 바로잡기
   - 결제예정일 새 카드 기준 재계산, 캘린더·잔여한도 실시간 자동 반영, card_logs에 '카드변경' 기록
9. **결재 승인 시 선택적 지시·요청사항 메모** (PR #85)
   - 승인 버튼 → 승인 모달(지시사항 textarea). 비워도 승인됨(선택)
   - 다단계 결재 시 승인자명과 함께 approval_note에 누적, 상세에 녹색 박스로 표시
   - SQL: db/approval_note.sql (approvals.approval_note 컬럼) — 실행 완료
10. **재고 수기 입출고/로그삭제 silent-fail 버그 수정** (PR #86)
   - 증상: 덴프스 수기 입고350→출고350 했는데 재고 차감 안 됨(경고도 없음)
   - 원인: handleMove가 재고 PATCH 성공 여부 미확인 → 실패해도 로그만 남기고 화면엔 반영된 듯 표시
   - 수정: return=representation으로 실제 반영 확인 후 로그 기록, 실패 시 중단+경고.
     handleDeleteLog 재고 원복 PATCH도 성공 확인 추가
   - 진단: 덴프스 로그가 전부 주문출고뿐, 수기 로그 없음 = 수기 이동이 저장 안 됐던 것.
     실제 재고는 수기로 맞춤(추가 보정 불필요). 앞으로 실패 시 경고 뜨게 처리됨
11. **업로드 이력 직접등록 건 상세 보기** (PR #88)
   - 직접등록 저장 시 배치 주문번호를 order_uploads.ref_order_number 기록
   - 업로드 이력에서 직접등록 행 클릭 → 등록 품목(현재상태) + 변경이력(등록·수정·삭제) 모달
   - 기존 직접등록 건은 created_at 기준 backfill로 연결(카카오스토어 1건은 주문 삭제돼 제외)
   - SQL: db/order_upload_ref.sql (order_uploads.ref_order_number) — 실행 완료
12. **쿠팡 로켓그로스 수수료율 등록** (데이터)
   - 매출현황 '수수료 미설정 몰' 경고 → mall_fees에 BNKNET·쿠팡로켓그로스 12% 등록. 공헌이익 정확화
13. **업로드 이력 삭제 시 주문·재고·매출 원상복구** (PR #89)
   - 업로드 이력 삭제 시 그 업로드로 저장된 주문 함께 삭제 + 재고 복구 + 매출 제외
   - 연결: 파일업로드=orders.upload_id(신규), 직접등록=ref_order_number(기존)
   - deleteOrdersWithRestore 재사용. 연결 못 찾으면 이력만 삭제(주문 유지)
   - SQL: db/order_upload_link.sql (orders.upload_id) — 실행 완료
   - ※ 직접등록은 기존건도 즉시 원복 / 파일업로드는 SQL 이후 저장분부터
14. **매출현황 사업자별 필터 버튼화** (PR #90)
   - 드롭다운 → 버튼 그룹(전체/BNKNET/SJ글로벌/더블아이/IX글로벌/미분류)
   - 버튼 클릭 시 매출·공헌이익·몰별·택배건수 전체가 그 사업자로 분리 집계

## 실행 SQL 총정리 (Supabase) — 전부 실행 완료
- `db/product_matches.sql` (product_matches + product_match_logs)
- `db/approval_note.sql` (approvals.approval_note)
- `db/order_upload_ref.sql` (order_uploads.ref_order_number) + 직접등록 backfill UPDATE
- `db/order_upload_link.sql` (orders.upload_id)
- 6/30 선결제 card_id 국민카드 교정 UPDATE
- mall_fees BNKNET·쿠팡로켓그로스 12% INSERT
- (그 외 코드 변경분은 SQL 불필요)

## 상태 / 팔로업
- PR #76·#77·#78·#80·#81·#82·#85·#86·#88·#89·#90 + 메모 PR(#79·#83·#84·#87) 모두 main 머지·반영 완료
- 즉시 처리 미결 없음 (덴프스 재고는 수기로 맞춤)
- 장기 팔로업: 판관비 반영 진짜 영업이익, 보안 RLS 마이그레이션, AI봇
