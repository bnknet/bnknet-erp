이번 작업 내용을 메모에 정리하고 저장(배포)한다.

절차:
1. 최신 메모 파일(docs/메모_YYYY-MM-DD.md, 없으면 새로 만듦)에 오늘 한 작업·결정·미완/다음 할 일을 사실대로 추가한다.
2. 실행이 필요한 SQL이 있으면 현황(완료/미실행)을 명확히 남긴다.
3. `/배포` 절차(커밋→PR→머지→재동기화)로 반영한다.
4. 사용자에게 간결히 요약 보고한다: 반영된 것 / 다음 최우선 / 사용자가 해야 할 것(SQL 실행 등).

$ARGUMENTS 가 있으면 메모 제목/맥락으로 사용한다.
